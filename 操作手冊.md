# 青山物料盤點 App — 開發與更新操作手冊

給自己看的中文版流程說明。技術細節給 Claude Code 看的版本在專案裡的 `CLAUDE.md`。

---

## 專案位置

```
~/Documents/InventoryApp/qingshan-inventory
```

線上網址（店員在用的）：
```
https://kurowei.github.io/qingshan-inventory/
```

GitHub 倉庫：
```
https://github.com/kurowei/qingshan-inventory
```

---

## 日常修改流程（標準四步驟）

每次想調整功能、修 bug、換物料清單，都照這個順序走：

### 1️⃣ 開啟 Claude Code，說出你要的修改

```bash
cd ~/Documents/InventoryApp/qingshan-inventory
claude
```

進去之後，直接用中文描述需求，例如：
```
幫我在盤點頁面加上一個「清空重填」的按鈕
```

### 2️⃣ 本機預覽，確認改動符合預期

**不要急著推上線**，先用預覽腳本檢查：
```bash
./preview.sh
```
會印出兩個網址：
- 電腦上看：`http://localhost:8000`
- 手機上看（要跟電腦連同一個 Wi-Fi）：`http://<你的IP>:8000`

拿手機實際操作一次，確認畫面、按鈕、功能都正常。

**測試完，按 `Ctrl + C` 關掉本機伺服器。**

### 3️⃣ 確認沒問題後，推上 GitHub

回到 Terminal（或直接請 Claude Code 幫你跑）：
```bash
git add .
git commit -m "說明這次改了什麼，例如：新增清空重填按鈕"
git push
```

### 4️⃣ 等 1-2 分鐘，正式上線

GitHub Pages 會自動重新部署，等 1-2 分鐘後，用手機重新整理正式網址：
```
https://kurowei.github.io/qingshan-inventory/
```
確認新功能已經生效。之後不用重傳連結給店員，網址不會變，店員手機上「加入主畫面」的捷徑會自動抓到新版本。

---

## 如果不滿意，想繼續調整

回到步驟 1，繼續跟 Claude Code 說要改哪裡，重複「改 → 預覽 → 推送」直到滿意為止。**沒有 push 之前，正式網址上的店員完全不受影響**，可以放心慢慢調。

---

## 常見情境對照表

| 我想做的事 | 該做什麼 |
|---|---|
| 改功能、修 bug | 走標準四步驟 |
| 更新物料清單（單價異動、新品項） | 把新版 Excel 傳給 Claude（對話），請它重新產生 `index.html` 內嵌資料，接著一樣走「預覽 → 推送」 |
| 只是想看看目前線上版本長怎樣 | 直接用手機開 `https://kurowei.github.io/qingshan-inventory/`，不用跑 preview.sh |
| 想確認本機改動有沒有存好，但還沒想 push | 跑 `./preview.sh` 看效果即可，不用執行 git 指令 |
| 電腦重開機、忘記在哪個資料夾 | 先 `cd ~/Documents/InventoryApp/qingshan-inventory` 再開始 |

---

## 給 GitHub 推送用的身分

這台 Mac 已經用 `gh auth login` 完成 GitHub 授權（帳號：kurowei），**不用每次重新登入**，直接 `git push` 就能用。

如果哪天出現「認證失敗」「Device not configured」之類的錯誤，代表授權過期或換了新電腦，重新跑一次：
```bash
gh auth login
```
跟著畫面選 GitHub.com → HTTPS → Login with a web browser，用瀏覽器登入授權即可。

---

## 小提醒

- 每次改完**務必先 `./preview.sh` 用手機測過**再推送，不要跳過這步直接 push，避免店員直接遇到有問題的版本
- commit message 建議寫清楚這次改了什麼，之後回頭找歷史紀錄比較好對照（例如 `git log` 可以看到所有修改紀錄）
- 資料是存在**店員手機的瀏覽器本機**，不是雲端資料庫，所以修改 `index.html` 本身不會影響店員手機上已經存的歷史盤點紀錄
