# 青山物料盤點 App — 開發與更新操作手冊

給自己看的中文版流程說明。技術細節給 Claude Code 看的版本在專案裡的 `CLAUDE.md`。

---

## 專案位置

```
~/Documents/InventoryApp/qingshan-inventory
```

線上網址（店員在用的）：
```
https://kurowei.github.io/qingshan-inventory/
```

GitHub 倉庫：
```
https://github.com/kurowei/qingshan-inventory
```

---

## 日常修改流程（標準四步驟）

每次想調整功能、修 bug、換物料清單，都照這個順序走：

### 1️⃣ 開啟 Claude Code，說出你要的修改

```bash
cd ~/Documents/InventoryApp/qingshan-inventory
claude
```

進去之後，直接用中文描述需求，例如：
```
幫我在盤點頁面加上一個「清空重填」的按鈕
```

### 2️⃣ 本機預覽，確認改動符合預期

**不要急著推上線**，先用預覽腳本檢查：
```bash
./preview.sh
```
會印出兩個網址：
- 電腦上看：`http://localhost:8000`
- 手機上看（要跟電腦連同一個 Wi-Fi）：`http://<你的IP>:8000`

拿手機實際操作一次，確認畫面、按鈕、功能都正常。

**測試完，按 `Ctrl + C` 關掉本機伺服器。**

### 3️⃣ 確認沒問題後，推上 GitHub

回到 Terminal（或直接請 Claude Code 幫你跑）：
```bash
git add .
git commit -m "說明這次改了什麼，例如：新增清空重填按鈕"
git push
```

### 4️⃣ 等 1-2 分鐘，正式上線

GitHub Pages 會自動重新部署，等 1-2 分鐘後，用手機重新整理正式網址：
```
https://kurowei.github.io/qingshan-inventory/
```
確認新功能已經生效。之後不用重傳連結給店員，網址不會變，店員手機上「加入主畫面」的捷徑會自動抓到新版本。

---

## 如果不滿意，想繼續調整

回到步驟 1，繼續跟 Claude Code 說要改哪裡，重複「改 → 預覽 → 推送」直到滿意為止。**沒有 push 之前，正式網址上的店員完全不受影響**，可以放心慢慢調。

---

## 新增/更新物料品項

物料清單資料是直接寫死在 `index.html` 裡的一個 JavaScript 陣列（`ITEMS`），不是連資料庫，
所以「新增品項」本質上是：**改 Excel → 轉成新的 ITEMS 陣列 → 走標準的「預覽→推送」流程**。

### 步驟

**1. 更新 Excel 主檔**

打開「盤點品項主檔.xlsx」，在「品項主檔」工作表最下方新增一列：
品號、品名、規格、單位、最新單價、最後叫貨日期、分類。
如果有進貨紀錄，也可以一併補在「單價歷史」工作表（這個不影響手機版 App，
純粹是維持你原本 Excel 記錄的完整性）。

**2. 把 Excel 放到 Mac 桌面，讓 Claude Code 直接處理轉換**

不用再跑回 Claude 對話視窗處理，直接在 Claude Code 裡說：

```
我在桌面放了一份更新後的「盤點品項主檔.xlsx」，幫我讀取「品項主檔」工作表，
更新 index.html 裡的 ITEMS 陣列，格式跟現有的保持一致
```

Claude Code 會自己讀取 Excel、重新產生 `ITEMS` 陣列並寫回 `index.html`。

**3. 照標準流程：先預覽，確認新品項有出現**

```bash
./preview.sh
```
用手機開啟印出來的網址，確認新增的品項出現在正確分類底下、單價顯示正確。

**4. 確認沒問題，推送上線**

```bash
git add .
git commit -m "新增物料品項：（寫新增了什麼）"
git push
```

等 1-2 分鐘，正式網址更新，店員之後開啟就會看到新品項。

### 小提醒

- 這個方式讓 Claude Code 直接讀取 Mac 本機的 Excel 檔案，**不需要再回到 Claude 對話視窗上傳檔案**，整個流程更快
- 如果 Claude Code 讀取時對 Excel 格式或欄位有疑問，會直接問你，照實回答即可
- 新增/更新品項後一樣要走「預覽 → 推送」，不要跳過測試直接上線

---

## 關掉 Claude Code 後，怎麼接續之前的進度

Claude Code 會自動記住每個專案資料夾裡的對話歷史，重開時可以直接接續，不用重講一次需求。

**步驟一：回到專案資料夾**

打開 Terminal（一般終端機，不是 Claude Code 裡面）：
```bash
cd ~/Documents/InventoryApp/qingshan-inventory
```

**步驟二：啟動並接續上一次的對話**
```bash
claude --continue
```
（簡寫 `claude -c`）會直接載入上次在這個資料夾裡對話的完整記錄，接著往下講就好。

**如果想挑選「哪一次」對話（不只是最近一次）**
```bash
claude --resume
```
（簡寫 `claude -r`）會跳出清單，列出這個資料夾過去的對話紀錄，用方向鍵選要接續哪一段。

**如果只是想開一個全新對話**（不接續之前的）
```bash
claude
```
直接這樣打就好，不用加參數。

**小提醒**：對話歷史是依「專案資料夾」區分的，只要在同一個 `qingshan-inventory` 資料夾裡執行，就找得到這裡的紀錄；換到別的專案資料夾，看到的會是那個專案自己的對話歷史。

---

## 常見情境對照表

| 我想做的事 | 該做什麼 |
|---|---|
| 改功能、修 bug | 走標準四步驟 |
| 更新物料清單（單價異動、新品項） | 見上方「新增/更新物料品項」章節，改 Excel → 交給 Claude Code 轉換 → 預覽 → 推送 |
| 只是想看看目前線上版本長怎樣 | 直接用手機開 `https://kurowei.github.io/qingshan-inventory/`，不用跑 preview.sh |
| 想確認本機改動有沒有存好，但還沒想 push | 跑 `./preview.sh` 看效果即可，不用執行 git 指令 |
| 電腦重開機、忘記在哪個資料夾 | 先 `cd ~/Documents/InventoryApp/qingshan-inventory` 再開始 |

---

## 給 GitHub 推送用的身分

這台 Mac 已經用 `gh auth login` 完成 GitHub 授權（帳號：kurowei），**不用每次重新登入**，直接 `git push` 就能用。

如果哪天出現「認證失敗」「Device not configured」之類的錯誤，代表授權過期或換了新電腦，重新跑一次：
```bash
gh auth login
```
跟著畫面選 GitHub.com → HTTPS → Login with a web browser，用瀏覽器登入授權即可。

---

## 小提醒

- 每次改完**務必先 `./preview.sh` 用手機測過**再推送，不要跳過這步直接 push，避免店員直接遇到有問題的版本
- commit message 建議寫清楚這次改了什麼，之後回頭找歷史紀錄比較好對照（例如 `git log` 可以看到所有修改紀錄）
- 資料是存在**店員手機的瀏覽器本機**，不是雲端資料庫，所以修改 `index.html` 本身不會影響店員手機上已經存的歷史盤點紀錄
